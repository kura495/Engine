#include "PPVinette.h"

void PPVinette::Update()
{
	materialData->x = 0.8f;
	/*if (Player::playerMoveValue) {
		materialData->x = 0.8f;
	}
	else {
		materialData->x = (std::max)(materialData->x - 0.01f, 0.0f);
	}*/

}
